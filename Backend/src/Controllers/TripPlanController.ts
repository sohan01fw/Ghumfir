import { RequestHandler } from "express";

//creating triplan route handler

export default function createTripPlan(req:any, res:any) {
  // Your code here
  console.log("This is from tripplan route handler");
}